import styled from 'styled-components';

const B = styled.b`
  display: block;
  font-weight: normal;
  text-decoration: none;
  color: #F7F0EA;
  font-size: 20px;
  margin: auto;
  font-weight: normal;
  font-size: regular;

`;

B.displayName = 'B';

export default B;
