import styled from 'styled-components';

const A = styled.a`
  text-decoration: none;
  color: #F7F0EA;
  font-size: 45px;
  font-weight: normal;
  font-size: regular;
  margin: 0 auto;
`;

A.displayName = 'A';

export default A;
