import styled from 'styled-components';

const A = styled.a`
  text-decoration: none;
  color: #F7F0EA;
  font-size: 40px;
  font-weight: normal;
  font-size: regular;
  margin-top: -100px;
`;

A.displayName = 'A';

export default A;
